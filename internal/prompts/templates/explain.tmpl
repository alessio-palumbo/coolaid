{{- define "content" }}

TARGET TYPE: {{.Target.Type}}
{{- if eq .Target.Type "function" }}
TARGET FUNCTION: {{.Target.Name}}
{{- else if eq .Target.Type "file" }}
TARGET FILE: {{.Target.Name}}
{{- end }}

Instructions:

{{ if eq .Target.Type "file" -}}
Explain the TARGET FILE at a high level:
- its overall purpose
- its main components (functions, structs, modules)
- how control flows through the file
- how it interacts with other parts of the system
- any important patterns or design decisions

{{ else if eq .Target.Type "function" -}}
Explain the function in detail:
- its purpose and responsibility
- its inputs and outputs
- step-by-step logic
- dependencies on other functions/types
- how it fits into the broader system
- if multiple functions share similar names, focus only on the TARGET FUNCTION.

{{ end -}}

{{- end }}
